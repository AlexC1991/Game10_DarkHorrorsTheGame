using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AmbientSound : MonoBehaviour
{
    public List<GameObject> soundLocations = new List<GameObject>();
    public List<ScriptableAudioFile> audioSounds = new List<ScriptableAudioFile>(); 


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
